{
  "name": "mp3-compress",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --compile --outfile compress.exe"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
